{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "npx nodemon -L server",
    "sass-watch":"sass --no-source-map --watch styles:public/styles" ,
    "local" : "npm-run-all --parallel sass-watch server"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.1",
    "bootstrap": "^5.2.3",
    "cookie-session": "^2.0.0",
    "ejs": "^3.1.8",
    "es6": "^0.0.7",
    "express": "^4.18.2",
    "pg": "^8.8.0",
    "sass": "^1.57.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5"
  }
}
